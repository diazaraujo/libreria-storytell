{
  "name": "libreria-storytell",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "quality:check": "python3 scripts/sync_quality.py --check"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
