# Economies with high inequality are concentrated in Latin America and Sub-Saharan Africa

| | |
|--|--|
| Chapter | **10** — Inequality |
| Graphic | `high_inequality_map` |
| Type | `vis` |
| Status | `ready` |
| Scenes | 0 |

## Description
The map shows the levels of inequality across the world.  Today, economies with high inequality (Gini index > 40) are concentrated in Latin America & the Caribbean and Sub-Saharan Africa.

## Data files copied
- `data/10_data_high_inequality_countries.csv`

## Scenes
_Single-view (no scroller scenes)._

## Implement
1. Edit `main.js` → `window.AtlasReplica.render`
2. Use `./data/*` and `config.json`
3. Reuse `shared/beeswarm.js`, `shared/wb-colors.js`
4. When done, set `"status": "ready"` in inventory (re-run or edit)

## Original
- Config extracted from `all_visualizations.json`
- Live Atlas may differ slightly from the static build
