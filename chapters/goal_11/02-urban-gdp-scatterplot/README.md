# Urbanization is associated with economic growth

| | |
|--|--|
| Chapter | **11** — Urban Development |
| Graphic | `urban_gdp_scatterplot` |
| Type | `vis` |
| Status | `ready` |
| Scenes | 0 |

## Description
This chart shows a scatter plot between urbanization rate and GDP per capita. Each dot is a country and a trend line shows that with higher urbanization rates, GDP per capita is also higher.

## Data files copied
- `data/11_data_urban_gdp_scatterplot.csv`

## Scenes
_Single-view (no scroller scenes)._

## Implement
1. Edit `main.js` → `window.AtlasReplica.render`
2. Use `./data/*` and `config.json`
3. Reuse `shared/beeswarm.js`, `shared/wb-colors.js`
4. When done, set `"status": "ready"` in inventory (re-run or edit)

## Original
- Config extracted from `all_visualizations.json`
- Live Atlas may differ slightly from the static build
