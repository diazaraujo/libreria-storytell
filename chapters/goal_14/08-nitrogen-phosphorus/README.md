# Increase in anthropogenic inputs of nitrogen and phosphorus

| | |
|--|--|
| Chapter | **14** — Ocean pollution |
| Graphic | `nitrogen_phosphorus` |
| Type | `vis` |
| Status | `ready` |
| Scenes | 0 |

## Description
The visualisation shows the development of anthropogenic nitrogen (N) and phosphorus (P) inputs to the biosphere (mean, Tg per year) since the industrial revolution (1860).[emphasis: ]

## Data files copied
- `data/14_data_nitrogen_phosphorus.csv`

## Scenes
_Single-view (no scroller scenes)._

## Implement
1. Edit `main.js` → `window.AtlasReplica.render`
2. Use `./data/*` and `config.json`
3. Reuse `shared/beeswarm.js`, `shared/wb-colors.js`
4. When done, set `"status": "ready"` in inventory (re-run or edit)

## Original
- Config extracted from `all_visualizations.json`
- Live Atlas may differ slightly from the static build
