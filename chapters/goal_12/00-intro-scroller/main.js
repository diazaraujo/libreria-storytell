/** Fossil-fuel subsidies: explicit time-series, country, and bubble encodings. */
window.AtlasReplica = {
  ready: true,
  isStub: false,
  render: (scene, ctx) => AtlasFossilSubsidies.render(scene, ctx),
};
