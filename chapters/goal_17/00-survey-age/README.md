# Many economies rely on survey data that are more than five years old

| | |
|--|--|
| Chapter | **17** — Data for Development |
| Graphic | `survey_age` |
| Type | `vis` |
| Status | `ready` |
| Scenes | 0 |

## Description
This chart shows the average age of latest surveys. Many countries rely on survey data that are more than five years old.

## Data files copied
- `data/17_data_survey_age.csv`

## Scenes
_Single-view (no scroller scenes)._

## Implement
1. Edit `main.js` → `window.AtlasReplica.render`
2. Use `./data/*` and `config.json`
3. Reuse `shared/beeswarm.js`, `shared/wb-colors.js`
4. When done, set `"status": "ready"` in inventory (re-run or edit)

## Original
- Config extracted from `all_visualizations.json`
- Live Atlas may differ slightly from the static build
