# Most regions are getting more arid

| | |
|--|--|
| Chapter | **15** — Life on land |
| Graphic | `aridity_difference` |
| Type | `vis` |
| Status | `ready` |
| Scenes | 0 |

## Description
World map showing where aridity increased in brown and where aridity decreased in green. Brown is dominating the map.

## Data files copied
_None matched automatically. Check Atlas `data/goal_*`._

## Scenes
_Single-view (no scroller scenes)._

## Implement
1. Edit `main.js` → `window.AtlasReplica.render`
2. Use `./data/*` and `config.json`
3. Reuse `shared/beeswarm.js`, `shared/wb-colors.js`
4. When done, set `"status": "ready"` in inventory (re-run or edit)

## Original
- Config extracted from `all_visualizations.json`
- Live Atlas may differ slightly from the static build
